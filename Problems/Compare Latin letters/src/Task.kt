import java.util.*

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)
    val ch1 = scanner.next().toUpperCase()
    val ch2 = scanner.next().toUpperCase()
    println(ch1 == ch2)
}